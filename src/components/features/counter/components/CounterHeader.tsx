const CounterHeader = () => {
  return (
    <div className="space-y-2 text-center">
      <h3 className="text-foreground text-2xl font-bold">
        Interactive Counter
      </h3>
      <p className="text-sm">
        Showing Y&apos;all how we should build a full component(hooks: only
        logics, small components: only UIs, final component: all small
        components, page: final component)
      </p>
    </div>
  )
}

export default CounterHeader
